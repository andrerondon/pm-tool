<h1 class='shadow-sm p-3 mb-5 bg-white rounded overflow-wrap'>tasks Page</h1>

<div class='row'>
  <div class='col-2'>
  </div>
  <div class='col-10'>
    <% @tasks.each do |task| %>
    <%# if you pass a instance to link_to it will create the href='/questions/:id' %>
   
      <%= link_to task do %>
        <li class='list-group-item'>
            <h1>
                <span class='badge badge-pill badge-info'>
                    <%= task.id %>
                </span>
                    <%= task.body %>
            </h1>

        </li>
      <% end %>
    

    <%# link_to just creates a `a tag` with the correct href. the below is equivalent to the above code %>
    
  </div>
</div>